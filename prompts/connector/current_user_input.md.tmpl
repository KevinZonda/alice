{{- /* Stage: Raw inbound message normalization; used by Processor.buildCurrentUserInput before reply-context enrichment. */ -}}
{{- if .HasIdentityContext -}}
`{{ .SenderName }}`说：{{ .SpeechText }}
{{- else -}}
{{ .BaseText }}
{{- end -}}
{{- if .Attachments }}

附加资源信息：
{{- range .Attachments }}
{{ .Index }}. 类型：{{ .Kind }}
{{- if .FileName }}
   文件名：{{ .FileName }}
{{- end }}
{{- if .ImageKey }}
   image_key：{{ .ImageKey }}
{{- end }}
{{- if .FileKey }}
   file_key：{{ .FileKey }}
{{- end }}
{{- if .LocalPath }}
   本地路径：{{ .LocalPath }}
{{- end }}
{{- if .DownloadError }}
   下载状态：失败（{{ .DownloadError }}）
{{- end }}
{{- end }}
{{- end -}}

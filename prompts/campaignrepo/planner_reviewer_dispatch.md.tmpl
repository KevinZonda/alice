评审第 {{ .PlanRound }} 轮计划是否已经可以交给人类审批，campaign repo 为 `{{ .CampaignRepo }}`。
Campaign ID: {{ .CampaignID }}。
下面提到的所有路径都相对于该 repo。
派发深度: {{ .DispatchDepth }}。
回合内修复预算: {{ .RepairBudget }}。
收尾自检预算: {{ .SelfCheckBudget }}。
本轮 receipt 路径: `{{ .ReceiptPath }}`。
上一次 receipt 路径: {{ .LastReceiptPath }}。
Fact digest: {{ .FactDigest }}。

Fact snapshot:
{{ .FactSnapshot }}

先读 `campaign.md`、`plans/proposals/round-{{ printf "%03d" .PlanRound }}-plan.md`、`plans/merged/master-plan.md`、phase / task 文档，并在需要时读取 source repo。

写作语言要求：
- review 正文和公开总结默认使用中文。
- 保留 frontmatter key、文件名、状态 / verdict 枚举、role 名、workflow 名，以及 `_templates/` 规定的 English section headings 不变。

重点检查：
- `master-plan.md` 必须是具体计划，而不是模板残留。
- proposal / master-plan / phases / tasks 在 phase 目标、task ID、depends_on、target_repos、write_scope、acceptance、parallelism 上必须一致。
- 每个 task 都必须是 `phases/Pxx/tasks/Txxx/` 目录，并包含 `task.md`、`context.md`、`plan.md`、`progress.md`、`results/`、`reviews/`。
- `_templates/` 规定的必需标题必须被保留。
- 标准 executor / reviewer block 必须使用 `workflow: code_army`，`write_scope` 要能真正约束改动范围，task package 必须达到 executor-ready。
- 在这个阶段把 `campaign.md` 视为 baseline metadata；除非 frontmatter / objective / source-repo facts 真不一致，不要要求 planner 去替换那里静态说明文字或占位提示。
- 本轮评审不要使用 `repo-lint --for-approval`；那个更严格的审批 gate 属于后续人工 `/alice approve-plan`，前提是 runtime 已把 `plan_status` 推进到 `plan_approved`。

粒度与可执行性判据：
- 如果 executor 看完 task 文件夹后，仍然需要追问目标文件、依赖、命令、验收方式、交接条件或真实写入范围，这个 task 至少是 `concern`。
- 如果一个 task 同时覆盖多个独立产物、多个互不相干 write_scope、多个松耦合 repo 改动，或者仍然停留在“先研究再决定怎么做”，这个 task 至少是 `concern`。
- 如果 Goal / Relevant Files / Execution Steps / Acceptance 里充满泛词，没有具体路径、命令、产物或验证方法，这个 task 至少是 `concern`。
- 如果 phase 切分解释不清依赖链、并行关系，或者对于一个明显复杂的目标只拆出了极少数宽泛 task，本轮 verdict 不应为 `approve`。
- 对于发现过粗的 task，review 里要点名具体 task ID，并明确建议按 repo / 模块 / 文件 / 产物 / 依赖中的哪条轴继续拆分。
- 这轮评审更严格：只有在你确认**完全没有剩余问题、没有注意事项、没有待人类补判边界**时，才允许写 `approve`。
- 只要还有任何问题，无论大小，都不允许带着备注 `approve`；必须写 `concern` 或 `blocking`，把 planner 打回去重做。

请写入 `plans/reviews/round-{{ printf "%03d" .PlanRound }}-review.md`，frontmatter 至少包含 `review_id: "plan-review-r{{ .PlanRound }}"`、`plan_round`、`reviewer.role: {{ .ReviewerRole }}`、`verdict`、`blocking`、`created_at`。
`created_at` 使用 RFC3339，例如 `2026-03-28T14:57:39+08:00`。
Verdict 只允许：`approve`、`concern`、`blocking`。缺少 merged-plan 内容、task package 仍是占位、artifact 不一致，通常判为 `concern` 而不是 `blocking`；任务粒度过粗、只能靠聊天补 context、验收不可观察，也通常至少是 `concern`。
如果 verdict 是 `approve`，review 正文里不应再保留非空的 `## Concerns` 或等价“注意事项/待补充”区段；有问题就不要批准。
运行 `alice-code-army repo-lint {{ .CampaignID }}` 或 `$ALICE_RUNTIME_BIN runtime campaigns repo-lint {{ .CampaignID }}`；任何 lint 失败至少都是 `concern`。
在结束本轮前，必须运行一次收尾自检命令并确认退出码为 0；如果失败，先修 review 文件或指出 planner 缺口，再重复运行，直到通过。自检命令：
`{{ .SelfCheckCommand }}`
在最后一次 self-check 之前，先把本轮结构化 receipt 写到 `{{ .ReceiptPath }}`；frontmatter 至少包含 `kind`、`plan_round`、`round`、`artifact_paths`、`requested_handoff`、`repair_attempts`、`self_check_attempts`、`created_at`。
不要先跑 lint / self-check 再写最终 review；必须在 review 文件写完后，重新跑最后一次 lint 和 self-check。
不要修改 source repo、`campaign.md` 或 proposal。你要自己写 review 文件，然后给出一条简短中文公开总结，包含 review 路径、verdict、repo-lint 结果，以及优先级最高的返工 task ID。

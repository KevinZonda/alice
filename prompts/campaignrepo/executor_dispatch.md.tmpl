继续以执行者身份推进这个 repo-first campaign。

Campaign repo: {{ .CampaignRepo }}
Task 文件: {{ .TaskFile }}
Task 目录: {{ .TaskDir }}
Task ID: {{ .TaskID }}
Task 标题: {{ .TaskTitle }}
执行角色: {{ .ExecutorRole }}
执行轮次: {{ .ExecutionRound }}
派发深度: {{ .DispatchDepth }}
回合内修复预算: {{ .RepairBudget }}
收尾自检预算: {{ .SelfCheckBudget }}
本轮 receipt 路径: {{ .ReceiptPath }}
上一次 receipt 路径: {{ .LastReceiptPath }}
收尾自检命令: {{ .SelfCheckCommand }}
目标 repos: {{ .TargetRepos | join ", " | default "-" }}
Source repos:
{{- if .SourceRepoRefs }}
{{- range .SourceRepoRefs }}
- {{ .RepoID }}: local_path={{ .LocalPath | default "-" }}, default_branch={{ .DefaultBranch | default "-" }}, task_branch={{ .WorkingBranch | default "-" }}, task_worktree={{ .WorktreePath | default "-" }}, repo_doc={{ .DocPath }}
{{- end }}
{{- else }}
- none declared
{{- end }}
工作分支: {{ .WorkingBranches | join ", " | default "-" }}
任务 worktree: {{ .WorktreePaths | join ", " | default "-" }}
Write scope: {{ .WriteScope | join ", " | default "-" }}
是否需要 source repo 改动: {{ if .SourceChangeRequired }}yes{{ else }}no（仅 campaign / archive 任务）{{ end }}
评审角色: {{ .ReviewerRole }}
报告摘要: {{ .ReportSnippet }}
评审状态: {{ .ReviewStatus }}
上次评审路径: {{ .LastReviewPath }}
产物自动重试次数: {{ .AutoRetryCount }}
集成冲突恢复次数: {{ .IntegrationRetryCount }}
阻塞指导次数: {{ .BlockGuidanceCount }}
上次阻塞原因: {{ .LastBlockedReason }}
上次阻塞代码: {{ .LastBlockedCode }}
Fact digest: {{ .FactDigest }}
Fact snapshot:
{{ .FactSnapshot }}
{{- if ne .LastHumanGuidancePath "-" }}
最近一次人工指导动作: {{ .HumanGuidanceStatus }}
最近一次人工指导摘要: {{ .LastHumanGuidanceSummary }}
最近一次人工指导文件: {{ .LastHumanGuidancePath }}
这是一次人工脱困后的恢复轮。开始执行前先读 guidance 文件；它记录了上一次卡住的原因和人类裁决。
{{- end }}
{{- if .ArtifactRepairOnly }}
这是一次 task-local artifact repair：优先补 task 结果、frontmatter、自检证据和 campaign-local 产物，不要无故扩大成新的 source diff 或新执行轮次。
{{- end }}
{{- if .IntegrationConflictRecovery }}
这是一次集成冲突恢复轮：上一轮把 task branch 回主线时发生了 merge conflict。你本轮的目标不是重做整个 task，而是在 `task_worktree` 中吸收默认分支的新变化、解掉冲突，并保留已批准改动的意图。
{{- end }}

执行规则：
1. 先读 `campaign.md`、task 目录里的 markdown，以及引用到的 source repo。
2. 你可以修改 `target_repos` 列出的 source repo，以及当前 task 目录；如果给出了 task-specific `task_worktree`，只允许在那条 worktree 里改 source repo，不要直接在 repo 根 worktree 上工作。
   - 所有 source-repo 编辑、测试和 git 检查都默认在 `task_worktree` 下执行，不要把 `local_path` 当成可写工作区。
   - 开始前先确认 `local_path` 干净；结束前再确认一次。只要发现 `local_path` 出现未提交改动，就说明你把改动泄漏到了根 worktree，必须先把泄漏改动迁回 `task_worktree` 并恢复 `local_path` clean，再继续；不要把这种可返工问题直接升级成 campaign 级 `blocked`。
3. 不要改 `reports/live-report.md`，也不要改无关 task 目录。
4. 把 task 文件夹当作执行合同面。先读 `task.md`、`context.md`、`plan.md`。如果 `review_status` 不是 `pending`，或者 `last_review_path` 已设置，先读那份 review，再去碰 source repo，并在本轮显式消化它的结论。{{ if .IntegrationConflictRecovery }}如果 `last_blocked_reason` 显示 merge conflict，把这轮视作 integration-recovery：优先在 `task_worktree` 中把默认分支的新变化合入或等价重放到 task branch，只处理 `write_scope` 范围内的冲突，不要直接在 repo 根 worktree 的默认分支上解冲突。{{ end }}
5. 一个完成的 executor round 必须且只能以一种合法交接状态结束：`review_pending`、`waiting_external` 或 `blocked`。结束时不要让 task 还停在 `executing` 或 `reviewing`。
6. 在把状态设为 `review_pending` 之前，先自己验证执行产物。保持 `last_run_path` 指向一个真实存在的 task-local result 文件，并清空 `owner_agent`、`lease_until`、`wake_at`、`wake_prompt`。
   - 如果这个 task 改了 source repo，把目标 repo 中真实可达的 git commit 写进 `head_commit`。
   - 如果这是 campaign-only 或 archive-only task，`head_commit` 保持空，不要伪造 commit 字符串。
   - 不要声称发生了并不存在的 source-repo 改动。
7. 如果任务在等待长时间外部作业，切到 `waiting_external`，清空 `owner_agent` 和 `lease_until`，然后设置 `wake_at` 与 `wake_prompt`。
8. 如果任务被阻塞，把状态设为 `blocked`，清空 `owner_agent`、`lease_until`、`wake_at`、`wake_prompt`，并在 task 文件里明确写清阻塞原因。Alice 在把任务视为终局阻塞前，可能会先走几轮 reviewer guidance。若你无法留下合法交接状态，发送 `/alice blocked ...` 或 `/alice needs-human ...`；不要静默留下非法活跃状态。
   - 但以下都不是 `blocked` / `needs-human`：远端作业还在跑、产物暂未落地、短暂环境/挂载抖动、路径/工件可通过继续执行修正、上一轮被打断、自检第一次未过但通过补齐状态还能重跑。
   - 这类“可等待 / 可继续运行 / 可重试解决”的问题，优先继续修正；如果只是等待外部作业，改成 `waiting_external` 并设置 `wake_at` / `wake_prompt`，随后重新跑 self-check。
9. executor 不得在 `reviews/` 下新建或修改 review 文件；只能读取已有 review。只有 reviewer 可以写新的 review 文件。
10. 最终回复前，必须运行一次收尾自检命令并确认退出码为 0；如果失败，先修 task/source-repo/result/frontmatter，再重复运行，直到通过。自检没过时，不允许结束本轮，也不要手工伪造 `self_check_*` 字段。
   - 先在回合内自愈：执行 -> 检查 -> 修复 -> 再检查。只要还没耗尽回合内修复预算，就不要把半成品状态直接抛给 runtime。
   - 在最后一次 self-check 之前，先写本轮结构化 receipt 到 `{{ .ReceiptPath }}`；frontmatter 至少包含 `kind`、`task_id`、`round`、`artifact_paths`、`requested_handoff`、`repair_attempts`、`self_check_attempts`、`created_at`。
   - receipt 里的 `artifact_paths` 必须覆盖这轮真实交接产物；如果要交给 reviewer，至少包含 `last_run_path`。
11. 自检前，自己先检查一遍 invariant：
    - 最终 task status 对 executor 交接是否合法
    - `owner_agent` / `lease_until` 是否与该状态匹配
    - 如果要交给 review，`last_run_path` 是否可解析
    - 如果需要 source-repo 证据，`head_commit` 是否真实存在
    - 不要把可等待、可继续运行或可重试解决的问题误升级成 `blocked` / `needs-human`
12. 继续以 campaign repo 为唯一事实源。
13. 你需要亲自完成 source repo 和 task 文件更新，然后给出一条简短中文公开总结，说明改了哪些文件、做了什么验证、最后 task 状态是什么，以及 self-check 是否通过。

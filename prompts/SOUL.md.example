# SOUL.md — 可选 bot 人格配置（YAML frontmatter + Markdown 正文）
#
# 读取时机：
# - 启动时如果文件不存在，Alice 会自动从内置模板创建
# - chat 场景：正文会注入到 prompt；YAML frontmatter 由 Alice 解析，不发给模型
# - work 场景：跳过 SOUL.md 注入
#
# YAML frontmatter 可选字段：
#   output_contract.suppress_token  静默 token，如 "[[NO_REPLY]]"
#   output_contract.hidden_tags     回复中需要剥离的标签，如 [reply_will, motion]
#   output_contract.reply_will_tag  回复意愿标签
#   output_contract.motion_tag      动作描述标签
#   image_refs                      引用图片（相对 SOUL.md 所在目录）
